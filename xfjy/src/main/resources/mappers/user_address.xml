<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xyc.proj" >
 <select id="getAddressByUser" resultType="java.util.HashMap"
		parameterType="java.util.HashMap">
	SELECT t.area_id,t.community_id,t.open_id,
	(select a.name from t_area a where a.id=t.area_id) as areaname,
	(select a.name from t_community a where a.id=t.community_id) as communityname,
	t.detail_address,
	t.user_name,
	t.mobile_no
	FROM xfjy.t_user_address t
	where open_id=#{openId}
		 
	</select>
 
  
</mapper>
